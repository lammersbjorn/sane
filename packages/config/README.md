# @sane/config

Configuration schema, defaults, TOML parsing, and Codex environment detection for Sane.

Verify with:

```bash
pnpm --filter @sane/config test
pnpm --filter @sane/config typecheck
```
